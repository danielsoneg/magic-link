apiVersion: v1
kind: Service
metadata:
  name: {{ include "magic-link.fullname" . }}
  labels:
    {{- include "magic-link.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "magic-link.selectorLabels" . | nindent 4 }}
